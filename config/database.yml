default: &default
  adapter: mysql2
  encoding: utf8
  pool: 5
  username: root
  password:
  localhost: localhost

development:
  <<: *default
  database: pro1903_booking_review_development

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: pro1903_booking_review_test

production:
  <<: *default
  database: <%= ENV['DATABASE_URL'] %>
